name: Demo CI Pipeline

on:
  workflow_dispatch:  # ⭐ Allows manual trigger from your IDE
  push:
    branches: [ main ]

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      
      - name: Install dependencies
        run: |
          echo "📦 Installing dependencies..."
          sleep 3
          echo "✅ Dependencies installed!"
      
      - name: Build project
        run: |
          echo "🔨 Building project..."
          sleep 5
          echo "✅ Build complete!"
      
      - name: Run tests
        run: |
          echo "🧪 Running tests..."
          sleep 4
          echo "✅ All tests passed!"

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to staging
        run: |
          echo "🚀 Deploying to staging..."
          sleep 6
          echo "✅ Deployment successful!"